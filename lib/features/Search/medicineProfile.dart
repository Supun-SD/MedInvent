import 'package:flutter/material.dart';

class MedicineProfile extends StatelessWidget {
  const MedicineProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
